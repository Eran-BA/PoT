# PoH (Pointer-over-Heads) Transformer - Requirements
# Pinned versions for reproducibility
# Python 3.9+

# Core deep learning
torch==2.0.1
transformers==4.30.2
datasets==2.14.0

# Configuration
pyyaml==6.0.1

# Data processing
numpy==1.24.3
scipy==1.10.1
pandas==2.0.3

# Visualization
matplotlib==3.7.2
seaborn==0.12.2

# Utilities
tqdm==4.65.0

# Maze generation (for maze-solving benchmarks)
maze-dataset>=1.0.0

# HPO (Hyperparameter Optimization)
optuna>=3.3.0
ray[tune]>=2.7.0
wandb>=0.15.0

# Development
pytest==7.4.0
pytest-cov==4.1.0
black==23.7.0
ruff==0.0.282

# Optional: RoPE support (for rotary positional encoding)
# rotary-embedding-torch==0.3.0

# Optional: CUDA acceleration (if available)
# Uncomment if using GPU:
# torch==2.0.1+cu118 --extra-index-url https://download.pytorch.org/whl/cu118
