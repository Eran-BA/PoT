# Sorting task: Length 20 (hard)

task: sorting
array_len: 20
mask_rate: 0.5

# Data
train_samples: 15000
val_samples: 1000
test_samples: 1000

# Model  
model: hrm_poh
d_model: 256
n_heads: 8
d_ctrl: 256
hrm_T: 4
iterations: 12
topk: null
temperature_init: 2.0
temperature_min: 0.7
entropy_reg: 1e-3

# Training
epochs: 50
batch_size: 48
lr: 3e-4
weight_decay: 0.01
use_amp: true
num_workers: 4

# Controller
controller_lr: 1e-4
controller_warmup_epochs: 5
deep_supervision: true

